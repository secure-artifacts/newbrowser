name: Build and Release with Attestation

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # 1. 对应前端 Windows 大按钮
          - os: windows-latest
            is_main: 'true'
            os_label: Windows_x64_普通电脑版
            py_ver: '3.10'

          # 2. 优雅避让，完美掉进【其他产物】下拉菜单
          - os: windows-11-arm
            is_main: 'false'
            os_label: Win_ARM64_高通骁龙本专用包
            py_ver: '3.11'

          # 3. 对应前端 macOS 大按钮
          - os: macos-latest
            is_main: 'true'
            os_label: macOS_M芯片新款
            py_ver: '3.10'

          # 4. 优雅避让，完美掉进【其他产物】下拉菜单
          - os: macos-15-intel
            is_main: 'false'
            os_label: Mac_Intel_老款电脑专用包
            py_ver: '3.10'

    runs-on: ${{ matrix.os }}
    permissions:
      id-token: write
      contents: read
      attestations: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.py_ver }}

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # 【核心修改】将编译出来的软件名称直接改为中文
          pyinstaller -F -w browser_Gui.py --name 浏览器痕迹分析 --clean --noconfirm
          
          if ('${{ matrix.is_main }}' -eq 'true') {
              $folderName = "浏览器痕迹分析-${{ github.ref_name }}-${{ matrix.os_label }}"
          } else {
              $folderName = "${{ matrix.os_label }}"
          }
          
          $targetPath = "dist_final/$folderName"
          New-Item -ItemType Directory -Force -Path $targetPath | Out-Null
          
          # 【核心修改】复制中文命名的 .exe 文件
          Copy-Item dist/浏览器痕迹分析.exe $targetPath/
          
          if (Test-Path "custom-domains.conf") {
              Copy-Item "custom-domains.conf" $targetPath/
          } else {
              Write-Host "错误: 未在仓库根目录检测到 custom-domains.conf 文件！"
              exit 1
          }
          
          Compress-Archive -Path $targetPath -DestinationPath "dist_final/${folderName}.zip" -Force

      - name: Build (macOS)
        if: runner.os == 'macOS'
        run: |
          # 【核心修改】将编译出来的 Mac 应用名称直接改为中文
          pyinstaller --onedir --windowed --name 浏览器痕迹分析 browser_Gui.py --clean --noconfirm
          
          if [ "${{ matrix.is_main }}" == "true" ]; then
              folder_name="浏览器痕迹分析-${{ github.ref_name }}-${{ matrix.os_label }}"
          else
              folder_name="${{ matrix.os_label }}"
          fi
          
          mkdir -p "dist_final/${folder_name}"
          # 【核心修改】复制中文命名的 .app 目录
          cp -r dist/浏览器痕迹分析.app "dist_final/${folder_name}/"
          
          if [ -f "custom-domains.conf" ]; then
              cp "custom-domains.conf" "dist_final/${folder_name}/"
          else
              echo "错误: 未在仓库根目录检测到 custom-domains.conf 文件!"
              exit 1
          fi
          
          cd dist_final && zip -r "${folder_name}.zip" "${folder_name}"

      - name: Attest Build Provenance
        uses: actions/attest-build-provenance@v2
        with:
          subject-path: dist_final/*.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os_label }}
          path: dist_final/*.zip

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
