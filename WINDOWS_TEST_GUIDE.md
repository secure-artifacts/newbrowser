# 浏览器痕迹分析 v1.1.10-test1：Windows 验证说明

> **用途**：本测试包用于验证少数 Windows 设备无法识别 Chrome 浏览器资料的问题。该版本不会上传浏览器数据；扫描和报告均在本机完成。请只在您有权限审计的设备上运行。

## 一、运行前准备

请解压 ZIP 文件到普通本地目录，例如桌面或下载目录。请勿把程序直接放在网络共享盘或 U 盘根目录运行。双击 `浏览器痕迹分析.exe` 即可启动；若 Windows Defender/SmartScreen 首次提示未知发布者，请按照公司的终端安全流程进行放行或由 IT 管理员审核，**不要**关闭全局防护。

建议在第一次测试时保持 Chrome 打开，以验证本版本的 SQLite WAL 一致性读取；第二次可关闭 Chrome 后再运行，用于对照识别结果和报告条数。

| 测试场景 | 操作 | 预期结果 |
|---|---|---|
| 常规 Chrome | 不勾选扩展兼容搜索，直接点击“开始检测” | 应识别当前 Windows 用户的 Chrome Stable Profile |
| Chrome 非稳定通道 | 使用 Beta / Dev / Canary / Chrome for Testing / Chromium 后直接检测 | 应显示对应浏览器名称与 Profile |
| Chrome 运行中 | 保持 Chrome 打开后检测 | 应能读取已提交历史；不会要求关闭 Chrome |
| 多 Profile | 登录多个 Chrome Profile 后检测 | 应列出 Default、Profile N 等多个配置 |
| 无 History Profile | 保留 `Bookmarks` 或 `Preferences`，清理 `History` 后检测 | 仍应识别 Profile 并扫描书签；诊断显示当前无 History |
| 自定义资料目录 | 点击“选择浏览器数据目录”，选择 Chrome 的 `User Data` 或具体 Profile 后开始检测 | 即使没有 `History`，只要存在可信 Profile 特征也应识别 |
| 远程/管理员运行 | 仅在已获审计授权时勾选“扩展兼容搜索”再检测 | 应额外检查同机其他 Windows 用户目录和本地/可移动盘的便携版路径 |

## 二、推荐测试顺序

首先不要勾选“扩展兼容搜索”，直接测试默认模式。默认模式只检查当前进程用户的标准浏览器路径，以控制隐私范围并避免扫描企业共享盘。如果默认模式已成功识别 Chrome，说明兼容性修复生效，无需启用扩展搜索。

若仍显示“未识别浏览器配置”，请按以下顺序处理：

1. 点击 **复制诊断信息**，将文本保存或粘贴给维护方。
2. 确认 Chrome 的实际资料目录：在 Chrome 地址栏访问 `chrome://version`，查看 **Profile Path**。
3. 点击 **选择浏览器数据目录**：可以选择 `Profile Path`、其上级 `User Data`，也可以选择更上级的 `Google` 或 `Chrome` 目录；发布候选版本会在所选目录下进行最多两层的受控定位，然后重新开始检测。
4. 只有在公司授权检查同机其他账户或便携版浏览器时，才勾选 **扩展兼容搜索**。

> 不要发送 `History`、`History-wal`、`Cookies`、`Login Data`、书签文件、截图中的网址或任何账号信息。复制出的诊断文本不包含这些内容，足以定位路径、权限、锁定或数据库结构问题。

## 三、如何判断本次测试是否成功

| 界面信息 | 含义 | 后续建议 |
|---|---|---|
| “已识别 N 个浏览器配置” | 浏览器目录发现成功 | 继续查看是否有规则匹配报告 |
| “已读取 N 个配置，未发现符合当前规则的留痕” | 浏览器读取成功，但当前规则没有命中 | 与旧版差异主要是识别/读取，不代表必然有命中结果 |
| “未识别浏览器配置” | 仍未发现可用 Profile | 复制诊断信息，并选择 `chrome://version` 中的 Profile Path 重试 |
| “一致性快照失败” | 直接只读和快照回退均被权限、锁或损坏问题阻断 | 复制诊断信息；可关闭 Chrome 后再试一次作对照 |
| 报告自动打开 | 已发现符合规则的记录 | 关闭报告和程序后，程序会尝试清理临时报告；若文件被浏览器占用，系统可能延迟释放 |

## 四、反馈模板

请仅复制以下模板中的非敏感信息；目录中的用户名可替换为 `<USER>`：

```text
测试包版本：v1.1.10-test1
Windows 版本：
程序运行方式：员工双击 / 远程工具 / 管理员 / 其他
Chrome 通道：稳定版 / Beta / Dev / Canary / Chromium / 不确定
默认模式识别配置数量：
手动选择 Profile Path 后识别配置数量：
是否勾选扩展兼容搜索：是 / 否
Chrome 打开时结果：
Chrome 关闭后结果：
复制诊断信息：
```

## 五、本版本的主要改动

v1.1.10-test1 保留无 History Profile 识别、书签独立优先扫描和单配置故障隔离。SQLite 数据库现在优先使用受限时的只读一致性事务读取活动 WAL，失败后才执行 Online Backup；打开、SQL 查询、快照、单 Profile、整次任务和单表均有防卡死保护。只有直接读取和快照都失败时才标记“部分完成”。诊断文本会说明实际使用的读取路径且不包含真实网址。
